@using shanuMVCUserRoles.Models;
@model IEnumerable<CategoryViewModel>

@foreach (var item in Model)
{
    <li>
        @Html.ActionLink(@item.Name, "Categories", "Shop", routeValues: new { id = @item.Id }, htmlAttributes: new { })
    </li>
}
